# Direct dependencies only — pip resolves and pins their own transitive
# deps (pydantic, urllib3, python-dateutil, etc.) at build time. Versions
# below match what has been running in production dev (see `pip freeze`
# on the dev machine, 2026-08-08); bump deliberately, not via `pip -U`.
alpaca-py==0.43.5
pandas==2.3.3
numpy==2.0.2
exchange_calendars==4.5.6
pandas_market_calendars==5.2.4
requests==2.32.5
pytz==2026.3.post1
tzdata==2026.3
flask==3.1.3
gunicorn==23.0.0
